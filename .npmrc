# npm network tuning — this machine's link to registry.npmjs.org is slow (~0.5 MB/s).
# Large native tarballs (next, sharp, swc) need generous fetch timeouts/retries.
fetch-retries=5
fetch-timeout=600000
fetch-retry-maxtimeout=120000
audit=false
fund=false
